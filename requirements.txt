pycaret>=3.3.0
numpy>=1.24.0
pandas>=2.0.0
openpyxl>=3.1.0

# RL training stack
stable-baselines3>=2.0.0
gymnasium>=1.0.0
torch>=2.0.0
matplotlib>=3.5.0
plotly>=5.0.0
scipy>=1.10.0
scikit-learn>=1.3.0
hmmlearn>=0.3.0
ruptures>=1.1.8
tqdm>=4.62.0
rich>=13.0.0

# Optional Gemini event labeling (Regime Check -> Refresh events)
google-genai>=1.0.0

# GUI
customtkinter>=5.2.0
Pillow>=9.0.0          # for logo/icon image loading (CTkImage)

# MT5 Python API tools (pull/live trading helpers; Windows only)
MetaTrader5>=5.0.45; platform_system == "Windows"

# ONNX export (for MT5 deployment)
onnx>=1.20.0
onnxscript>=0.7.0
onnxruntime>=1.20.0

# Document/slide generation (optional)
python-pptx>=0.6.21
python-docx>=1.1.0

# Tensorboard (optional, for training logs)
tensorboard>=2.10.0
